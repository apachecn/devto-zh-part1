# CSS:从这个到离开

> [https://dev . to/leandrocr/CSS-do-hate-this-m-a-can-let me-2ea 7](https://dev.to/leandrocrs/css-do-odeio-essa-m-a-pode-deixar-comigo-2ea7)

> *   From the excellent [Post](https://blog.10pines.com//2017/01/24/css-from-i-hate-it-to-i-actually-like-this-thing/) Do [Ignacio Piatanida](https://blog.10pines.com/authors/ignacio-piantanida/) No [10-pin blog](https://blog.10pines.com/) , January 24th, 2017
> 
> 翻译改编而来

我记得我作为网络开发者的最初几天，当我接了一个看起来如此简单的任务时:*∞我们需要在这张表格下面加上一个小红色按钮。他需要靠右对齐。啊！啊！并垂直集中化□□□□□□□□□□□□。我被诅咒了。花了几个小时破坏布局，修复布局，然后每次我修改 CSS 时再破坏布局…我们以前都经历过这种情况。*

幸运的是，阅读这篇文章后，您将会学到一些技巧，帮助您(a)在完全掌握 CSS 的道路上。

博拉·拉！

### 0。忘记旧浏览器

我不认为这是一个提示，更确切地说是 0，但这是我在开始一个新项目时一直考虑的问题。

尽量避免支持旧浏览器。看看的使用统计数据，平衡**的比例【扩大用户范围 x 开发中的额外成本】**。支持比 IE 10 更老的东西会让人头疼。

当然，这并不总是可能的。我知道一家公司要求所有不直接与 T.I .合作的员工使用 IE 7，所以所有内部工具都必须在 dit 中工作。如果这是你的情况，我真的为你感到抱歉。我帮不了你。

如果您已经拥有站点及其统计信息，请注意版本和浏览器在业务上下文中的行为方式。例如，在我参与的其中一个项目中，IE 用户的访问率很低。但是，当您按浏览器查看销售转换等数据时，IE 用户的转换率要高得多，这表示销售百分比很高。

支持值得的东西。

### 1。了解优先权规则

我忍不住把它刮下来。如果您不明白为什么某个属性会应用于另一个属性，则您将始终与框架、库甚至是您自己项目中的大量 CSS 发生冲突。

假设您有以下 HTML 和 CSS 代码:

```
<div id="container" class="my-container">
  I want my <span class="text red">color</span>
</div>

<style>
#container span {
  color: blue;
}

.my-container .text.red {
  color: red;
}
</style> 
```

你认为文字的颜色会是什么？

如果你的回答是*∞我不在乎，我干脆放一个吧！重要的是我☆t1〖嗯，祝你过得很 IE 6 滋润的生活。*

对其他打电话的人来说，答案是蓝色的。无论在第二条规则中添加多少选择器，都决不会使文本变为红色。

关键是不同种类的选择器(“t0”# id、**、**、**、**或**具有不同的优先权。每个选择器都有一个□，而且重量越大越好。**

 **uma boa fonte de estudo e compreenso sobre o assuntoéo curtíssimo post do Steven Bradley，([理解 CSS 中的风格优先:特异性、继承性和级联](http://vanseodesign.com/css/css-specificity-inheritance-cascaade/))。

### 2。使用预处理程序

现在很难找到只使用纯 CSS 而不使用预处理程序的 webapp，但是如果要启动新项目，请尽快添加预处理程序。*mixes*、变量、继承和 *import* 将有助于保持其模块化风格，从头到尾不重复(干原理)。

我总是选择 [Sass](http://sass-lang.com/) ，这是目前最常用的 CSS 预处理程序，其次是 [Less](http://lesscss.org/) 。

### 3。抓住 Flexbox 进行定位

使用 [flexbox](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Flexible_Box_Layout/Using_CSS_flexible_boxes) 可以构建一致的响应布局，只需少量的规则即可。首次使用后，再也不想再使用*或 [*自举网格系统*](http://getbootstrap.com/css/#grid) 。*

 *我喜欢这个游戏，即‘t0’flex box froggy . com 学习基础知识。

请务必小心每个浏览器及其前缀的兼容性。这就引出了下一个提示…

### 4。不要手动键入浏览器前缀

为每个浏览器添加前缀会导致重复，从而导致错误。

是一种帮助您处理浏览器之间微小差异的工具。基于“[”吸管“](http://caniuse.com/)”的基础上，定义要支持的浏览器版本，自动修复程序将搜索 CSS 文件，并为每个浏览器添加所需的属性。忘了所有这些垃圾吧**—moz**或**—WebKit**。

无论您在开发过程中使用哪种堆栈，webpack、gulp 或 grunt，都很容易集成。

如果你不想改变管道里的任何东西，至少要用一些东西比如‘t0’bourbon’t1’，一个为你添加所有前缀的 sass mixin 图书馆。

### 5。请勿使用任何类别名称，请使用方法

随着项目的发展，您的样式表页面的数量和复杂性可能会成为一个问题。如果不小心，向类中添加简单的属性可能会对站点的其他部分产生巨大的影响，而您甚至无法想象。

使用特定方法将防止这种错误。同时，您将为代码库定义一个模式，以确保来自不同开发人员的许多样式之间的一致性。

有多种方法可以解决这个问题，比如好的，OOCSS，SMACSS 等等……他唯一熟悉的就是好的。

com[well(box，Element，modify)](http://getbem.com/)，则定义类型为**的类。bloco _ _ elemento 修饰符**。例如，如果您有一个标有公司徽标的标题和一个登录按钮，您将会有像**、**、**、**或**这样的类。隐藏的 __btn-logar 标题。【隐藏的】因此，每次查看样式表中的选择器时，您都会知道它干扰了哪些元素以及在何处找到它。**

此外，good 建议了一个折叠的类结构(而不是像**那样嵌套所有内容。head . BTN-logar**。这样，您就可以从 HTML 中的结构中取消样式的关联，并且样式将始终以相同的方式工作(当然，继承属性除外)。

### [](#6-entenda-as-diferentes-unidades-de%C2%A0medida)6。医疗保险公司

你知道 **px** 、**在**和 **rem** 之间的区别吗？

你知道视口单位(“t0”VWe**VH**是什么吗？

在工具箱中添加超过 **px** 和 **%** 的单元，将使您能够扩展可以做的事情，特别是在处理响应站点时。了解更多关于它们的信息[在这里](https://developer.mozilla.org/en-US/docs/Web/CSS/length)。

### 7。了解位置的所有不同值

你有没有想过为什么有时会把的位置定义为**绝对**，元素直接飞到页面顶端，而有时只是停留在同一个地方呢？

关键在于‘t0’位置值如何相互作用。**绝对**、**相对**、**固定**或**静态**有各自的使用案例，您必须了解每一个使用案例的位置。

### [](#8-pseudoseletores)8。伪选择者

CSS 选择器不仅仅是类、id 和元素。在 CSS 规范的每个新版本中，都会出现一个新的伪选择器，允许您使用纯 CSS 执行以前只能使用 Javascript 或在服务器上处理的操作。

**:最后类型**、**:最后子**、**:**是一直使用的伪选择者的一些例子。[此网站](https://kimblim.dk/css-tests/selectors/)显示了一个巨大的伪选择器列表，包括定义、示例以及每个浏览器版本中的支持。

这里的秘密不多，但请确保您了解风格和**计算**标签所提供的一切。它们非常有助于了解某些属性被过度写入的位置，并快速测试新内容。

此外，在您的开发流程中确保正在生成“**源映射”(**source maps)，以便您可以直接从浏览器查看正在设置该属性的文件/行。

### [](#conclus%C3%A3o)结论

了解所有这 9 点之后，您将非常了解如何控制您的风格，特别是在布局问题上，我认为布局问题是最复杂的部分。

当然，CSS 世界远不止布局，还有许多其他更高级的主题要涵盖，如动画、过渡、转换、[新网格布局、](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Grid_Layout)等等……但是在这方面，您需要了解每个属性所做的工作以及它们不同的值是什么，但是

成为 CSS 专业人员需要大量的时间和实践，但在坚实的基础上不会是一个艰难的旅程。

而且，在本文的结尾，没有 GIF，任何有关 CSS 的帖子都无法完成。他永远不会变老！

[https://medium . com/media/9 ee5b 80578406d 432 a 1906 ea 570676 AC/href](https://medium.com/media/9ee5b80578406d432a1906ea570676ac/href)***